int perteUsine(arbre usine ){
  if (usine==NULL){
    return -1
  }
  else{
    return som_perte(usine) ; // faut faire une fonction qui additionne toute les pertes presente dans tous les sous arbe comme pour la fonction prefix mais avec des arbre qui n'ont pas de limite de nombre de sous arbre et sa renvoie un int 
  }
}
// je sais pas si c'est comme ca qu'il faut faire
